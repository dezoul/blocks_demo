---
label: banner-with-header
hide_body: false
fields:
- type: textarea
  name: text
  label: banner text
  config:
    wysiwyg: true
    schema:
      format: markdown
    required: true
- type: file
  label: background image
  name: banner-pic
- name: links
  type: blocks
  label: links
  template_types:
  - link
  config:
    min: 
    max: 
