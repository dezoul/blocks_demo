---
label: clickable-grid
hide_body: false
fields:
- type: text
  name: link
  label: link
- type: textarea
  label: pre-grid text
  name: text
  description: text above the grid
  config:
    wysiwyg: false
    schema:
      format: markdown
- name: sec
  type: blocks
  label: col
  template_types:
  - hover-item
  config:
    min: 
    max: 
