---
label: hover-grid
hide_body: false
fields:
- type: textarea
  label: pre-grid text
  name: text
  description: text above the grid
  config:
    wysiwyg: false
    schema:
      format: markdown
- name: col
  type: blocks
  label: column
  template_types:
  - hover-item
  config:
    min:
    max:
