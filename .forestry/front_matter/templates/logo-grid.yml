---
label: logo-grid
hide_body: false
fields:
- type: textarea
  label: cta text
  name: text
  config:
    wysiwyg: false
    schema:
      format: markdown
- type: blocks
  name: logo
  label: logo's
  template_types:
  - single-logo
  config:
    min: 
    max: 
